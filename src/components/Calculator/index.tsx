import { Grid } from "@material-ui/core";
import React from "react";

import useStyles from "./useStyles";

export function Calculator() {
  const classes = useStyles();

  return <Grid className={classes.root}>index</Grid>;
}
